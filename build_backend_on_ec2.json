{
    "InstanceIds": ["i-0f8ade6523273cdfd"],
    "DocumentName": "AWS-RunShellScript",
    "Parameters": {
        "commands": [
            "cd /home/ubuntu && git pull origin main || true",
            "cd /home/ubuntu/backend && docker build -t talentpilot-backend:latest . || true",
            "docker tag talentpilot-backend:latest 095695999221.dkr.ecr.us-east-1.amazonaws.com/talentpilot-backend:latest || true",
            "docker restart backend || (cd /home/ubuntu && docker compose up -d)"
        ]
    }
}
